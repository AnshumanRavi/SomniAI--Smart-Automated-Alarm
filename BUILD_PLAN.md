# SomniAI — Build Plan (Research Paper)

Getting SomniAI to a publishable paper. Not a product launch.

**How to use this file.** Give me one prompt at a time, in order. Each has a
`Verify` block — don't move on until it passes. If a step fails, tell me what you
saw rather than skipping ahead.

**Target:** a methods paper validated on public sleep datasets. Submission in
roughly three months of steady part-time work.

---

## The one thing that decides whether this gets published

Reviewers do not read your code. They read your evaluation.

The idea is sound and the implementation is already better built than most work
that gets accepted. What would sink it today is a single sentence any reviewer
would write: *"the system is evaluated only on synthetic data generated by the
authors."*

So the work ahead is **evaluation, not features**. Every prompt below either
produces evidence a reviewer will weigh, or protects the correctness of a number
you are going to report. Nothing else earns its place.

## Explicitly out of scope

Cut from the earlier product plan, because none of it changes a review outcome:

| Dropped | Why |
| --- | --- |
| Capacitor / mobile wrapper | Only needed for a deployment study with real participants |
| Background alarm delivery | Same — irrelevant without human subjects |
| Twilio, Google Calendar, Health/Fit | Real integrations feed a product, not a public-dataset evaluation |
| Rate limiting, Sentry, uptime monitoring | Operational concerns for users you don't have yet |
| Store listings, privacy policy, beta recruitment | No launch is happening |

If you later want a top-tier HCI venue (IMWUT, CHI), the deployment study comes
back and drags background alarms with it. That extends this work rather than
replacing it — do this first regardless.

## Where to send it

Verify the current call for papers before committing; conference deadlines are
fixed and you should pick one and work backwards from it.

- **Conferences:** IEEE ICHI, PervasiveHealth, IEEE EMBC, ACM SAC. CHI
  Late-Breaking Work is a legitimate faster stepping stone.
- **Journals:** Expert Systems with Applications, Knowledge-Based Systems,
  IEEE JBHI, Computers in Biology and Medicine.

Note that "Q1" is a journal quartile — conferences rank by CORE A\*/A/B/C
instead. The CORE A/A\* venues in this space all expect human-subjects evidence,
which is the branch you've deferred.

---

## Phase 0 — Protect the numbers you'll report

A bug in the scheduler or the confidence gate means the results in your paper are
wrong. That's a retraction risk, not a code-quality preference. Scoped
deliberately tight: the modules whose behaviour the paper makes claims about, and
nothing else.

- [x] **1. Vitest and the wake-confidence gate** — *done, 19 tests*

- [x] **2. Escalation and scheduler logic** — *done, 51 tests*

- [x] **3. The AI Brain** — *done, 60 tests. Verified by mutation: removing the
  habit attribution fails 2 tests; inverting the bedtime search fails 4.*

---

## Phase 1 — Get real data under the models

The critical path.

> **Dataset pivot (2026-09-10).** MESA never arrived — no reply, no access. It
> is no longer required. MMASH was investigated and rejected: one night per
> person made the habit features underivable and left ~22 usable rows.
> **LifeSnaps** and **PMData** are both downloaded, verified, and need no access
> request. Measured counts after building the loaders: 69 participants and
> 3,551 person-nights (LifeSnaps), 16 and 1,881 (PMData) — the raw record counts
> quoted when the datasets were chosen were higher, before multi-episode nights
> were folded. See [`docs/feature-mapping-lifesnaps-pmdata.md`](docs/feature-mapping-lifesnaps-pmdata.md).

- [x] **4. Request the long-lead dataset** — *drafted in
  [`docs/nsrr-mesa-request.md`](docs/nsrr-mesa-request.md); no reply received.
  Superseded by the pivot above, kept in case MESA is wanted later.*

- [x] **5. Map your features onto reality** — *done for MMASH (rejected), then
  for LifeSnaps and PMData against the real files. 4–5 direct, 5 derivable,
  6 absent. Habit partition 2 of 4; controllable partition 3 of 7.*

- [x] **6. Load the datasets** — *done. 149 tests pass (132 unit + 17
  integration against the real downloads).*

  `ai-brain/datasets/` — `pmdata.py`, `lifesnaps.py`, shared `schema.py` and
  `derive.py`, plus `bson_stream.py`, a minimal streaming BSON reader written
  because LifeSnaps hides sleep timing in a 9.7 GB mongodump and pymongo is not
  a dependency.

  | | Person-nights | Participants | Complete cases |
  | --- | --- | --- | --- |
  | LifeSnaps | 3,551 | 69 | 1,780 |
  | PMData | 1,881 | 16 | 1,210 |

  Three corrections came out of building it, all folded back into
  [`docs/feature-mapping-lifesnaps-pmdata.md`](docs/feature-mapping-lifesnaps-pmdata.md):
  `age` is a de-identified band (`<30`/`>=30`) in LifeSnaps and absent from
  PMData, so **neither dataset yields a numeric age**; LifeSnaps has 69
  participants with sleep, not 71; and covariate coverage is far better than the
  CSV-level figures implied (`resting_hr` 98.9%, not 59.7%) because nights with
  sleep records are nights the watch was worn. Only `stress_level` is genuinely
  sparse.

  `TestNoLeakage` in `tests/test_derive.py` pins the habit features to
  **strictly prior nights** — the one silent bug here that would make every
  number in the paper optimistic.

- [x] **7. Design the wake-success proxy** — *done, with a negative result that
  changes the paper's framing. Full write-up in
  [`docs/wake-proxy-validation.md`](docs/wake-proxy-validation.md).*

  `ai-brain/datasets/wake_proxy.py`, 27 unit tests + 7 real-data checks.

  **Sensitivity: passes.** Baseline choice moves the positive rate by 3.4 points;
  night-by-night agreement across settings is 0.86–0.94. Labels 89% of LifeSnaps
  nights (rate 0.746) and 95% of PMData (0.666).

  **Circularity: passes.** Correlation with `sleep_consistency` is +0.089 /
  +0.053 — the label is not a restatement of a model input.

  **Construct validity: fails.** No association with self-reported sleep quality,
  readiness or tiredness (all |d| < 0.1, two of three the wrong way). It does
  track objective timing (corr with sleep duration +0.18 / +0.37).

  **Consequence:** waking later than habit is not a bad outcome — most late
  wakes are lie-ins. The target must be renamed from *wake success* to
  **wake-time regularity**, predicting P(waking within N minutes of habitual
  wake time). The inverse-planning method is unchanged; only the claim changes.
  Steps 9 onward and the manuscript should use the new framing.

---

## Phase 2 — Retrain and report honestly

- [x] **8. Retrain the sleep-duration model** — *done. The gap is far larger
  than "worse". Write-up in
  [`docs/sleep-duration-results.md`](docs/sleep-duration-results.md).*

  `ai-brain/training/train_real.py`, 22 unit tests + 4 real-data checks.

  | | MAE (h) | R² | vs mean baseline |
  | --- | --- | --- | --- |
  | Synthetic panel (same 7 features) | 0.403 | +0.702 | +45.6% |
  | LifeSnaps (mean of 8 splits) | 1.016 | +0.186 | +15.0% |
  | PMData | 1.072 | **−0.090** | +3% |

  **The synthetic panel overstates predictability roughly 4× in R².** On PMData
  the model is worse than predicting the mean. Across LifeSnaps splits R² runs
  −0.08 to +0.40, so no single-split figure is a result. An oracle using each
  test participant's own mean beats the model in 5 of 8 splits — most
  predictable variance is between people, not between nights within a person.

  Two guards, both tested: splits are by participant (asserted across 15 seeds),
  and `assert_no_leakage` refuses `time_in_bed_hours`, `sleep_efficiency` and
  `wake_time_hour` as predictors, since duration is nearly time-in-bed times
  efficiency.

  **Consequence:** the planner inverts a model explaining ~19% of variance.
  Report planner behaviour conditional on that, and treat frequent
  infeasibility verdicts as correct behaviour rather than a defect.

- [x] **9. Retrain the wake-success model** — *done. The off-ramp triggered.
  Write-up in [`docs/wake-regularity-results.md`](docs/wake-regularity-results.md).*

  | | AUC | Balanced acc. | Accuracy | Majority | Δ |
  | --- | --- | --- | --- | --- | --- |
  | Synthetic | 0.761 | 0.700 | 0.699 | 0.515 | +18.4 pts |
  | LifeSnaps | 0.660 | 0.615 | 0.716 | 0.748 | −3.2 pts |
  | PMData | 0.675 | 0.594 | 0.683 | 0.685 | −0.2 pts |

  Real signal, replicated: AUC above chance in **13 of 13 splits** across both
  cohorts. But unweighted accuracy beats majority by only **+1.0 point**, which
  is not a defensible headline. Class weighting is not the cause — unweighted
  AUC is unchanged at 0.663.

  `sleep_duration_hours` is refused as a predictor here: with `bedtime_hour` it
  reconstructs wake time and therefore the label.

---

## Framing decision (2026-09-10)

Steps 7–9 all point one way: **wrist-wearable features predict sleep outcomes
far more weakly than a synthetic panel implies.** The label tracks schedule
regularity rather than wellbeing; sleep duration falls from R² 0.70 to 0.19;
wake regularity from AUC 0.76 to 0.66.

**Chosen framing: infeasibility-first.** The contribution is a planner that
knows when it cannot promise. Weak models are the *motivation*, not a
limitation to apologise for — most systems emit a number regardless of whether
they can support it, and this one declines. The bedtime recommendation is a
secondary output; the refusal and its attribution are the claim.

This changes what steps 10–12 measure. Bedtime-accuracy baselines are no longer
the point; **calibration and refusal quality are.**

---

## Phase 3 — Build the evidence a reviewer wants

> Rewritten for the infeasibility-first framing. The old versions measured
> bedtime accuracy, which the step 8–9 results no longer support as a headline.

- [x] **10. Calibration and refusal quality** — *done. Write-up in
  [`docs/calibration-refusal-results.md`](docs/calibration-refusal-results.md).*

  `ai-brain/evaluation/planner_eval.py`, 22 tests.

  **Calibration holds: ECE 0.0148** over 3,180 held-out person-nights. The 0.93
  bin observes 0.929. Reported with Brier (0.1727 vs 0.1898 for a base-rate
  predictor) because always predicting the base rate scores ECE 0.0000 —
  calibrated and useless. Honest summary: **well calibrated, weakly informative.**

  **Refusal beats always-promising decisively.** Over-promise rate at target
  0.95: always-promise and cycle-calculator 1.000, inverse 0.040. At 0.90:
  0.867 against 0.068.

  **But inversion itself is not testable here.** `forward_only` scores better
  than `inverse` at every target, and that is a measurement artifact: forward
  predicts the night that actually happened, while inversion promises
  conditional on a bedtime change nobody made. The counterfactual is
  unobservable in retrospective data. Validated: calibration, and refusal versus
  always-promising. Not testable: the bedtime recommendation itself.

  *Also fixed here:* `datasets/cache.py`. Parsing LifeSnaps costs ~296 s per run
  because of the 9.7 GB mongodump; cached reads take 0.07 s. Every experiment
  was paying that, which made the planner look slow when it was not.

- [x] **11a. Validate the inversion in simulation** — *done. Write-up in
  [`docs/simulation-validation-results.md`](docs/simulation-validation-results.md).*

  `ai-brain/evaluation/simulation.py` + `scenarios.py`, 17 tests. Seven
  ground-truth models × six targets, graded against exhaustive search.

  | Property | Result |
  | --- | --- |
  | Bedtime sweep finds the latest feasible bedtime | **100%** (42/42) |
  | Promises only what is reachable (**soundness**) | **100%** — 0 violations |
  | Lever set exactly minimal when it succeeds | **100%** (13/13) |
  | Refuses only the genuinely unreachable (**completeness**) | 76% — 10 false refusals |

  **Sound but incomplete** — it never over-promises, but is conservative. The
  cause is the iteration budget, not the greedy strategy: a traced case needed
  4 rounds and `max_rounds=3` stopped it one round short of the target.

  Raising `max_rounds` 3 → 8 cuts false refusals from 10 to 3 and **never
  introduces a broken promise**. Recommended, left unchanged pending your
  decision since it alters production behaviour.

- [x] **11b. Matched-subset analysis and ablations** — *done. Write-up in
  [`docs/ablation-matched-results.md`](docs/ablation-matched-results.md).*

  `ai-brain/evaluation/ablation.py` + `matched.py`, 16 tests.

  **Ablations — every component has a distinct job:**

  | Ablation | Verdict correct | False refusals | Broken promises | Bedtime optimal |
  | --- | --- | --- | --- | --- |
  | full planner | 0.929 | 3 | **0** | 1.00 |
  | no bedtime sweep | 0.881 | 5 | 0 | **0.69** |
  | no coordinate ascent | **0.762** | **10** | 0 | 1.00 |
  | no controllable/habit partition | 0.857 | 3 | **3** | 1.00 |
  | no attribution | 0.929 | 3 | 0 | 1.00 |

  The sweep buys optimality (minimality 0.95 → 0.56 without it), ascent buys
  completeness, and **the partition is the only component whose removal breaks
  soundness** — the mechanism the invention disclosure called distinctive turns
  out to be the one keeping the planner honest. Attribution changes no verdict,
  only whether the user is told why (2 habit causes named vs 0).

  **Matched subset — inconclusive, and that is the finding.** Direction positive
  at all four tolerances (d 0.17–0.40) but every CI crosses zero, with only 9–11
  participants contributing a paired difference. Underpowered by construction,
  and not randomised.

  Its real value is sizing the next study: detecting an effect this size needs
  **~50–75 participants**. `PRODUCT_PLAN.md` Phase F currently proposes 15–30,
  which would be underpowered — update it before running anything.

- [x] **12. Statistics** — *done. Write-up in
  [`docs/statistics-results.md`](docs/statistics-results.md).*

  `ai-brain/evaluation/statistics.py`, 17 tests. All uncertainty comes from
  resampling **participants**, never rows — a participant contributes ~50
  correlated nights.

  **The signal survives proper intervals.** R² [0.075, 0.297] excludes zero and
  AUC [0.610, 0.710] excludes chance, so "weak but real" is now supported rather
  than asserted from point estimates that swung −0.077 to +0.395 across splits.

  **Calibration is less precise than it looked.** ECE 0.0148 carries a
  bootstrap interval reaching **0.057** — still good, but the point estimate
  alone overstated it.

  **The two outcomes have different structure.** Unconditional ICC is 0.336 for
  sleep duration (a third is a stable person-level trait, formalising step 8's
  oracle result) but only 0.060 for wake regularity. Wake regularity is almost
  entirely night-to-night, which arguably makes it the better target for a
  system that intervenes on nights.

  Two predictors survive in both models by different estimators: later bedtime
  (−0.295 SD of sleep; OR 0.68 for regularity) and resting heart rate. **No
  habit feature reaches significance in either.**

  *Trap recorded:* the first fit reported ICC 0.000 with intervals spanning ±6.5
  million. `lbfgs` reported convergence while sitting on the boundary; `powell`
  and an independent ANOVA both find the variance is real. Models now run under
  `powell` with a `boundary_solution` flag and a test pinning the choice.

---

## Phase 4 — Write it

- [x] **13. Figures** — *done. Notes in [`docs/figures.md`](docs/figures.md).*

  `ai-brain/evaluation/figures.py`, 13 tests. Six figures, PNG and PDF, drawn
  from the same functions that produce the numbers in `docs/` so a figure and
  its table cannot drift apart.

  | # | Figure | Carries |
  | --- | --- | --- |
  | 1 | Inverse search, two panels | The mechanism *and* the refusal |
  | 2 | Synthetic vs real | The ~4× overstatement in R² |
  | 3 | Calibration | Reliability diagram with ECE interval |
  | 4 | Refusal quality | Over-promise rate by planner and target |
  | 5 | Ablation | Each component, split by failure mode |
  | 6 | Across splits | Why one split is not a result |

  **Figure 1 has two panels deliberately** — under infeasibility-first the
  refusal is the contribution, so it gets equal space. **Figure 5 separates
  false refusals from broken promises** rather than plotting one accuracy
  number: the two failures are not equivalent, and separating them is what makes
  the habit partition's unique role visible at a glance.

  **Greyscale is enforced, not hoped for.** Nothing is distinguished by hue;
  every series carries a grey level, a line style or hatch, and a marker.
  `check_greyscale_separation` asserts a minimum luminance gap of 0.12 — the
  palette scores 0.18 — and a test proves the check has teeth by rejecting two
  blues that look distinct on screen and identical in print.

  `figures/` is gitignored: regenerable artifacts, and binaries in review diffs
  are noise. `matplotlib` went into `requirements-research.txt`, not the service
  requirements.

- [x] **14. The manuscript** — *Methods, Results and Limitations drafted in
  [`paper/draft.md`](paper/draft.md) (~3,300 words).*

  Markdown for now; converts to IEEEtran at submission. Every number traces to a
  table in `docs/` and regenerates from `evaluation/run_study.py`.

  **The verify step caught real gaps.** Auditing the draft against the code's
  actual constants showed the Methods section named the seven controllable
  levers but not their directions, ranges or step sizes — so nobody could have
  reimplemented Algorithm 2 from it. Also missing: the 20.5 °C set-point, the
  habit reference values, the three distinct gain thresholds, and the chronotype
  cut points. §3.4 now tabulates all of them.

  It also surfaced a genuine ambiguity: there are **two different trailing
  windows** — 28 nights for the wake-proxy baseline, 7 for the habit features —
  which the first draft silently conflated. Now stated as a contrast.

  Limitations names all nine, including the three the verify step demands: the
  constructed proxy and its null construct validity, the absence of a deployment
  study, and the synthetic-to-real gap — the last framed not only as a finding
  but as a limitation of *this* work, since the planner's thresholds were tuned
  against a simulation now known to be optimistic.

- [x] **15. Reproducibility package** — *done. Instructions in
  [`REPRODUCE.md`](REPRODUCE.md).*

  `ai-brain/reproduce.py` is the single command: checks the environment, runs
  the suite, regenerates every table and figure, and compares against the values
  the paper quotes — exiting non-zero on a mismatch, so it works in CI rather
  than only by eye. Plus `requirements-lock.txt` (exact pins),
  `fetch_datasets.py` (resumable, integrity-checked, prints citations).

  **The clean-machine test earned its place twice.** It first showed none of
  steps 11b–15 was committed, so a fresh clone had nothing to run. With that
  fixed, it showed `run_study.py` accepted `--figures` but never generated any —
  an earlier edit had silently failed and `reproduce.py` reported success anyway
  because it only checked tables. Both now fixed and verified end to end.

  It also surfaced a Windows gotcha worth recording: numpy's nested test
  fixtures push a long clone path past the 260-character `MAX_PATH` limit, and
  pip fails partway through leaving a misleading `No module named
  'numpy._utils'`. Clone to a short path.

  **Verified from a fresh clone, fresh virtualenv, pinned versions, no data
  directory:** all four checked values reproduce exactly — sweep optimality
  1.000, verdict accuracy 0.929, broken promises 0, false refusals 3.

---

## Phase 5 — Submit

- [ ] **16. Hostile review**  ← **you are here**

  ```
  Review the full manuscript as a hostile reviewer would, and list every rejection reason you can find.
  ```

  **Verify:** Every objection either fixed or answered in the paper. The proxy
  label and the synthetic-data history will be the first two attacked.

- [ ] **17. Preprint and submission**

  ```
  Prepare the arXiv preprint, the submission package, and the cover letter for the target venue.
  ```

  **Verify:** Submitted before the CFP deadline, with the venue's formatting
  requirements met exactly.

---

## Rules

- **One prompt at a time.** Each step's verification protects the next.
- **Evaluation beats features.** If a proposed piece of work doesn't change a
  number in the paper or protect one, it doesn't belong in this plan.
- **Report what you find.** Real-data results that are worse than synthetic are
  a finding. Tuning until the numbers look good is how papers get retracted.
- **Never report a number no test protects.**
